const username: string = "Renish";
const age: number = 22;

function greet(name: string): string {
  return `Hello ${name}`;
}

console.log(greet(username));
console.log(`Age: ${age}`);

showCompany();
